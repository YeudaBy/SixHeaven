package com.yeudaby.sixheaven.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────
val Burgundy      = Color(0xFF800A2C)   // Primary – meat / brand
val BurgundyLight = Color(0xFFFFE4ED)
val SkyBlue       = Color(0xFF38BDF8)   // Secondary – dairy
val SkyBlueLight  = Color(0xFFE0F2FE)
val SageGreen     = Color(0xFF059669)   // Neutral / pareve
val SageGreenLight = Color(0xFFD1FAE5)

// ── Surface / glass ───────────────────────────────────────────────────────────
val OffWhite      = Color(0xFFF8FAFC)   // Background canvas
val GlassWhite    = Color(0xB3FFFFFF)   // 70% white – glass card fill
val GlassBorder   = Color(0xCCFFFFFF)   // 80% white – glass card border

// ── Background blob colors ───────────────────────────────────────────────────
// Meat state
val BlobWarmA  = Color(0xFFFFBECF)   // Rose
val BlobWarmB  = Color(0xFFFDE8CC)   // Peach
// Dairy state
val BlobCoolA  = Color(0xFFBAE6FD)   // Sky blue
val BlobCoolB  = Color(0xFFCBF0FF)   // Pale cyan
// Neutral state
val BlobFreshA = Color(0xFFA7F3D0)   // Mint
val BlobFreshB = Color(0xFFD1FAE5)   // Pale green

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFF0F172A)
val TextSecondary = Color(0xFF64748B)
val TextDisabled  = Color(0xFFCBD5E1)
